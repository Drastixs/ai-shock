# wireless_test.py — Phase 1 exit-gate proof.
# Edit the message below on your PC, sync it to the board over Wi-Fi (WebREPL),
# then `import wireless_test` on the board and confirm the new text prints.
# No USB data path involved — this file travels over the network.

print("=== wireless sync OK: edited on PC, ran on the board over Wi-Fi ===")
print("change this line, re-send, re-import, and watch it update")
